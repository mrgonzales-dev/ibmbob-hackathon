"""bob-pr components package.

Each module owns one concern: database, pull requests, decisions,
snapshots (shadow copies), rendering, server, and the CLI.
bob_pr.py re-exports the public API so callers use one import.
"""
